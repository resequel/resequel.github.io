Query#1: WITH rev_agg AS (SELECT o.o_custkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue FROM (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) o JOIN (SELECT l_orderkey, l_extendedprice, l_discount FROM lineitem WHERE l_returnflag = &&&_D) l ON o.o_orderkey = l.l_orderkey GROUP BY o.o_custkey) SELECT c.c_custkey, c.c_name, r.revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM rev_agg r JOIN customer c ON r.o_custkey = c.c_custkey JOIN nation n ON c.c_nationkey = n.n_nationkey ORDER BY r.revenue DESC LIMIT ###_B;
Query#2: WITH o AS (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH), l AS (SELECT l_orderkey, l_extendedprice * (###_A - l_discount) AS rev FROM lineitem WHERE l_returnflag = &&&_D), rev_agg AS (SELECT o.o_custkey, sum(l.rev) AS revenue FROM o JOIN l ON o.o_orderkey = l.l_orderkey GROUP BY o.o_custkey) SELECT c.c_custkey, c.c_name, r.revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM rev_agg r JOIN customer c ON r.o_custkey = c.c_custkey JOIN nation n ON c.c_nationkey = n.n_nationkey ORDER BY r.revenue DESC LIMIT ###_B;
Query#3: SELECT c.c_custkey, c.c_name, r.revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM (SELECT o.o_custkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue FROM (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) o JOIN (SELECT l_orderkey, l_extendedprice, l_discount FROM lineitem WHERE l_returnflag = &&&_D) l ON o.o_orderkey = l.l_orderkey GROUP BY o.o_custkey) r JOIN customer c ON r.o_custkey = c.c_custkey JOIN nation n ON c.c_nationkey = n.n_nationkey ORDER BY r.revenue DESC LIMIT ###_B;
Query#4: WITH rev_agg AS MATERIALIZED (SELECT o.o_custkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue FROM orders o JOIN lineitem l ON o.o_orderkey = l.l_orderkey WHERE o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH AND l.l_returnflag = &&&_D GROUP BY o.o_custkey) SELECT c.c_custkey, c.c_name, r.revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM rev_agg r JOIN customer c ON r.o_custkey = c.c_custkey JOIN nation n ON c.c_nationkey = n.n_nationkey ORDER BY r.revenue DESC LIMIT ###_B;
Query#5: WITH l_agg AS (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_returnflag = &&&_D GROUP BY l_orderkey), o AS (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) SELECT c.c_custkey, c.c_name, sum(l.rev) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c JOIN o ON c.c_custkey = o.o_custkey JOIN l_agg l ON o.o_orderkey = l.l_orderkey JOIN nation n ON c.c_nationkey = n.n_nationkey GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#6: SELECT c.c_custkey, c.c_name, sum(l.rev) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c JOIN (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) o ON c.c_custkey = o.o_custkey JOIN (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_returnflag = &&&_D GROUP BY l_orderkey) l ON o.o_orderkey = l.l_orderkey JOIN nation n ON c.c_nationkey = n.n_nationkey GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#7: WITH l_agg AS (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_returnflag = &&&_D GROUP BY l_orderkey) SELECT c.c_custkey, c.c_name, sum(l.rev) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c JOIN orders o ON c.c_custkey = o.o_custkey JOIN l_agg l ON o.o_orderkey = l.l_orderkey JOIN nation n ON c.c_nationkey = n.n_nationkey WHERE o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#8: WITH o_l AS (SELECT o.o_custkey, l.rev FROM (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) o JOIN (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_returnflag = &&&_D GROUP BY l_orderkey) l ON o.o_orderkey = l.l_orderkey) SELECT c.c_custkey, c.c_name, sum(ol.rev) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c JOIN o_l ol ON c.c_custkey = ol.o_custkey JOIN nation n ON c.c_nationkey = n.n_nationkey GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#9: SELECT c.c_custkey, c.c_name, sum(ol.rev) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c JOIN (SELECT o.o_custkey, l.rev FROM (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) o JOIN (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_returnflag = &&&_D GROUP BY l_orderkey) l ON o.o_orderkey = l.l_orderkey) ol ON c.c_custkey = ol.o_custkey JOIN nation n ON c.c_nationkey = n.n_nationkey GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#10: WITH o AS (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH), l AS (SELECT l_orderkey, l_extendedprice * (###_A - l_discount) AS rev FROM lineitem WHERE l_returnflag = &&&_D) SELECT c_custkey, c_name, sum(l.rev) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer JOIN o ON c_custkey = o.o_custkey JOIN l ON l.l_orderkey = o.o_orderkey JOIN nation ON c_nationkey = n_nationkey GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#11: SELECT c.c_custkey, c.c_name, sum(l.rev) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c JOIN (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) o ON c.c_custkey = o.o_custkey JOIN (SELECT l_orderkey, l_extendedprice * (###_A - l_discount) AS rev FROM lineitem WHERE l_returnflag = &&&_D) l ON o.o_orderkey = l.l_orderkey JOIN nation n ON c.c_nationkey = n.n_nationkey GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#12: WITH l AS (SELECT l_orderkey, l_extendedprice, l_discount FROM lineitem WHERE l_returnflag = &&&_D) SELECT c_custkey, c_name, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer JOIN orders o ON c_custkey = o.o_custkey JOIN l ON l.l_orderkey = o.o_orderkey JOIN nation ON c_nationkey = n_nationkey WHERE o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#13: SELECT c_custkey, c_name, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer JOIN orders o ON c_custkey = o.o_custkey JOIN (SELECT l_orderkey, l_extendedprice, l_discount FROM lineitem WHERE l_returnflag = &&&_D) l ON l.l_orderkey = o.o_orderkey JOIN nation ON c_nationkey = n_nationkey WHERE o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#14: WITH o AS (SELECT o_orderkey, o_custkey FROM orders WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH) SELECT c_custkey, c_name, sum(l_extendedprice * (###_A - l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer JOIN o ON c_custkey = o.o_custkey JOIN lineitem ON l_orderkey = o.o_orderkey JOIN nation ON c_nationkey = n_nationkey WHERE l_returnflag = &&&_D GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#15: SELECT c_custkey, c_name, sum(l_extendedprice * (###_A - l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer JOIN orders ON c_custkey = o_custkey JOIN lineitem ON l_orderkey = o_orderkey JOIN nation ON c_nationkey = n_nationkey WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH AND l_returnflag = &&&_D GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#16: SELECT c_custkey, c_name, sum(l_extendedprice * (###_A - l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer INNER JOIN orders ON c_custkey = o_custkey INNER JOIN lineitem ON l_orderkey = o_orderkey INNER JOIN nation ON c_nationkey = n_nationkey WHERE o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH AND l_returnflag = &&&_D GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#17: SELECT c.c_custkey, c.c_name, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM orders o INNER JOIN lineitem l ON o.o_orderkey = l.l_orderkey INNER JOIN customer c ON o.o_custkey = c.c_custkey INNER JOIN nation n ON c.c_nationkey = n.n_nationkey WHERE o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH AND l.l_returnflag = &&&_D GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#18: SELECT c.c_custkey, c.c_name, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, c.c_acctbal, n.n_name, c.c_address, c.c_phone, c.c_comment FROM customer c, orders o, lineitem l, nation n WHERE c.c_custkey = o.o_custkey AND l.l_orderkey = o.o_orderkey AND c.c_nationkey = n.n_nationkey AND o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH AND l.l_returnflag = &&&_D GROUP BY c.c_custkey, c.c_name, c.c_acctbal, c.c_phone, n.n_name, c.c_address, c.c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#19: SELECT c_custkey, c_name, sum(l_extendedprice * (###_A - l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM nation n JOIN customer c ON n.n_nationkey = c.c_nationkey JOIN orders o ON c.c_custkey = o.o_custkey JOIN lineitem l ON o.o_orderkey = l.l_orderkey WHERE o.o_orderdate >= date &&&_A AND o.o_orderdate < date &&&_B + interval &&&_C MONTH AND l.l_returnflag = &&&_D GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;
Query#20: SELECT c_custkey, c_name, sum(l_extendedprice * (###_A - l_discount)) AS revenue, c_acctbal, n_name, c_address, c_phone, c_comment FROM customer, orders, lineitem, nation WHERE c_custkey = o_custkey AND l_orderkey = o_orderkey AND o_orderdate >= date &&&_A AND o_orderdate < date &&&_B + interval &&&_C MONTH AND l_returnflag = &&&_D AND c_nationkey = n_nationkey GROUP BY c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment ORDER BY revenue DESC LIMIT ###_B;