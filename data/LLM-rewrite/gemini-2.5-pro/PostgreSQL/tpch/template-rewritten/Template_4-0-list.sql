Query#1: WITH c AS (SELECT c_custkey FROM customer WHERE c_mktsegment = &&&_A), o AS (SELECT o_orderkey, o_custkey, o_orderdate, o_shippriority FROM orders WHERE o_orderdate < date &&&_B), l AS (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_shipdate > date &&&_C GROUP BY l_orderkey) SELECT l.l_orderkey, l.rev AS revenue, o.o_orderdate, o.o_shippriority FROM c JOIN o ON c.c_custkey = o.o_custkey JOIN l ON l.l_orderkey = o.o_orderkey ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#2: SELECT l.l_orderkey, l.rev AS revenue, o.o_orderdate, o.o_shippriority FROM (SELECT c_custkey FROM customer WHERE c_mktsegment = &&&_A) c JOIN (SELECT o_orderkey, o_custkey, o_orderdate, o_shippriority FROM orders WHERE o_orderdate < date &&&_B) o ON c.c_custkey = o.o_custkey JOIN (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_shipdate > date &&&_C GROUP BY l_orderkey) l ON l.l_orderkey = o.o_orderkey ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#3: WITH co AS (SELECT o.o_orderkey, o.o_orderdate, o.o_shippriority FROM customer c JOIN orders o ON c.c_custkey = o.o_custkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B), l AS (SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS rev FROM lineitem WHERE l_shipdate > date &&&_C GROUP BY l_orderkey) SELECT co.o_orderkey AS l_orderkey, l.rev AS revenue, co.o_orderdate, co.o_shippriority FROM co JOIN l ON co.o_orderkey = l.l_orderkey ORDER BY revenue DESC, co.o_orderdate LIMIT ###_B;
Query#4: WITH c AS (SELECT c_custkey FROM customer WHERE c_mktsegment = &&&_A), o AS (SELECT o_orderkey, o_custkey, o_orderdate, o_shippriority FROM orders WHERE o_orderdate < date &&&_B), l AS (SELECT l_orderkey, l_extendedprice * (###_A - l_discount) AS rev FROM lineitem WHERE l_shipdate > date &&&_C) SELECT l.l_orderkey, sum(l.rev) AS revenue, o.o_orderdate, o.o_shippriority FROM c JOIN o ON c.c_custkey = o.o_custkey JOIN l ON l.l_orderkey = o.o_orderkey GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#5: SELECT l.l_orderkey, sum(l.rev) AS revenue, o.o_orderdate, o.o_shippriority FROM (SELECT c_custkey FROM customer WHERE c_mktsegment = &&&_A) c JOIN (SELECT o_orderkey, o_custkey, o_orderdate, o_shippriority FROM orders WHERE o_orderdate < date &&&_B) o ON c.c_custkey = o.o_custkey JOIN (SELECT l_orderkey, l_extendedprice * (###_A - l_discount) AS rev FROM lineitem WHERE l_shipdate > date &&&_C) l ON l.l_orderkey = o.o_orderkey GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#6: WITH filtered_orders AS (SELECT o.o_orderkey, o.o_orderdate, o.o_shippriority FROM orders o JOIN customer c ON o.o_custkey = c.c_custkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B) SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, fo.o_orderdate, fo.o_shippriority FROM filtered_orders fo JOIN lineitem l ON fo.o_orderkey = l.l_orderkey WHERE l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, fo.o_orderdate, fo.o_shippriority ORDER BY revenue DESC, fo.o_orderdate LIMIT ###_B;
Query#7: SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, co.o_orderdate, co.o_shippriority FROM (SELECT o.o_orderkey, o.o_orderdate, o.o_shippriority FROM customer c JOIN orders o ON c.c_custkey = o.o_custkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B) co JOIN lineitem l ON l.l_orderkey = co.o_orderkey WHERE l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, co.o_orderdate, co.o_shippriority ORDER BY revenue DESC, co.o_orderdate LIMIT ###_B;
Query#8: WITH l AS (SELECT l_orderkey, l_extendedprice, l_discount FROM lineitem WHERE l_shipdate > date &&&_C) SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM customer c JOIN orders o ON c.c_custkey = o.o_custkey JOIN l ON l.l_orderkey = o.o_orderkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#9: SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM orders o JOIN lineitem l ON l.l_orderkey = o.o_orderkey WHERE o.o_custkey IN (SELECT c_custkey FROM customer WHERE c_mktsegment = &&&_A) AND o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#10: SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM orders o JOIN lineitem l ON l.l_orderkey = o.o_orderkey WHERE EXISTS (SELECT 1 FROM customer c WHERE c.c_custkey = o.o_custkey AND c.c_mktsegment = &&&_A) AND o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#11: SELECT lo.l_orderkey, sum(lo.rev) AS revenue, lo.o_orderdate, lo.o_shippriority FROM (SELECT l.l_orderkey, l.l_extendedprice * (###_A - l.l_discount) AS rev, o.o_custkey, o.o_orderdate, o.o_shippriority FROM lineitem l JOIN orders o ON l.l_orderkey = o.o_orderkey WHERE o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C) lo JOIN customer c ON lo.o_custkey = c.c_custkey WHERE c.c_mktsegment = &&&_A GROUP BY lo.l_orderkey, lo.o_orderdate, lo.o_shippriority ORDER BY revenue DESC, lo.o_orderdate LIMIT ###_B;
Query#12: SELECT l.l_orderkey, sum(calc.rev) AS revenue, o.o_orderdate, o.o_shippriority FROM customer c JOIN orders o ON c.c_custkey = o.o_custkey JOIN lineitem l ON l.l_orderkey = o.o_orderkey CROSS JOIN LATERAL (SELECT l.l_extendedprice * (###_A - l.l_discount) AS rev) calc WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#13: WITH c AS MATERIALIZED (SELECT c_custkey FROM customer WHERE c_mktsegment = &&&_A), o AS MATERIALIZED (SELECT o_orderkey, o_custkey, o_orderdate, o_shippriority FROM orders WHERE o_orderdate < date &&&_B) SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM c JOIN o ON c.c_custkey = o.o_custkey JOIN lineitem l ON l.l_orderkey = o.o_orderkey WHERE l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#14: SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM lineitem l JOIN orders o ON l.l_orderkey = o.o_orderkey JOIN customer c ON o.o_custkey = c.c_custkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#15: SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS revenue, o_orderdate, o_shippriority FROM customer INNER JOIN orders ON c_custkey = o_custkey AND c_mktsegment = &&&_A AND o_orderdate < date &&&_B INNER JOIN lineitem ON l_orderkey = o_orderkey AND l_shipdate > date &&&_C GROUP BY l_orderkey, o_orderdate, o_shippriority ORDER BY revenue DESC, o_orderdate LIMIT ###_B;
Query#16: SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM customer c JOIN orders o ON c.c_custkey = o.o_custkey JOIN lineitem l ON l.l_orderkey = o.o_orderkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#17: SELECT l.l_orderkey, sum(l.l_extendedprice * (###_A - l.l_discount)) AS revenue, o.o_orderdate, o.o_shippriority FROM orders o INNER JOIN customer c ON o.o_custkey = c.c_custkey INNER JOIN lineitem l ON o.o_orderkey = l.l_orderkey WHERE c.c_mktsegment = &&&_A AND o.o_orderdate < date &&&_B AND l.l_shipdate > date &&&_C GROUP BY l.l_orderkey, o.o_orderdate, o.o_shippriority ORDER BY revenue DESC, o.o_orderdate LIMIT ###_B;
Query#18: SELECT lineitem.l_orderkey, sum(lineitem.l_extendedprice * (###_A - lineitem.l_discount)) AS revenue, orders.o_orderdate, orders.o_shippriority FROM customer JOIN orders ON customer.c_custkey = orders.o_custkey JOIN lineitem ON lineitem.l_orderkey = orders.o_orderkey WHERE customer.c_mktsegment = &&&_A AND orders.o_orderdate < date &&&_B AND lineitem.l_shipdate > date &&&_C GROUP BY lineitem.l_orderkey, orders.o_orderdate, orders.o_shippriority ORDER BY revenue DESC, orders.o_orderdate LIMIT ###_B;
Query#19: SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS revenue, o_orderdate, o_shippriority FROM lineitem, orders, customer WHERE c_mktsegment = &&&_A AND o_orderdate < date &&&_B AND l_shipdate > date &&&_C AND c_custkey = o_custkey AND l_orderkey = o_orderkey GROUP BY l_orderkey, o_orderdate, o_shippriority ORDER BY revenue DESC, o_orderdate LIMIT ###_B;
Query#20: SELECT l_orderkey, sum(l_extendedprice * (###_A - l_discount)) AS revenue, o_orderdate, o_shippriority FROM customer, orders, lineitem WHERE c_mktsegment = &&&_A AND c_custkey = o_custkey AND l_orderkey = o_orderkey AND o_orderdate < date &&&_B AND l_shipdate > date &&&_C GROUP BY l_orderkey, o_orderdate, o_shippriority ORDER BY revenue DESC, o_orderdate LIMIT ###_B;