Query#1: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.sum_rev_a ELSE l.line_cnt * ###_B END) / sum(l.sum_rev_c) AS promo_revenue FROM (SELECT l_partkey, count(*) AS line_cnt, sum(l_extendedprice * (###_A - l_discount)) AS sum_rev_a, sum(l_extendedprice * (###_C - l_discount)) AS sum_rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH GROUP BY l_partkey) l JOIN part p ON l.l_partkey = p.p_partkey;
Query#2: WITH l AS (SELECT l_partkey, count(*) AS line_cnt, sum(l_extendedprice * (###_A - l_discount)) AS sum_rev_a, sum(l_extendedprice * (###_C - l_discount)) AS sum_rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH GROUP BY l_partkey) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.sum_rev_a ELSE l.line_cnt * ###_B END) / sum(l.sum_rev_c) AS promo_revenue FROM l JOIN part p ON l.l_partkey = p.p_partkey;
Query#3: WITH l AS (SELECT l_partkey, l_extendedprice, l_discount FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.l_extendedprice * (###_A - l.l_discount) ELSE ###_B END) / sum(l.l_extendedprice * (###_C - l.l_discount)) AS promo_revenue FROM l JOIN part p ON l.l_partkey = p.p_partkey;
Query#4: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.l_extendedprice * (###_A - l.l_discount) ELSE ###_B END) / sum(l.l_extendedprice * (###_C - l.l_discount)) AS promo_revenue FROM (SELECT l_partkey, l_extendedprice, l_discount FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) l JOIN part p ON l.l_partkey = p.p_partkey;
Query#5: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.rev_a ELSE ###_B END) / sum(l.rev_c) AS promo_revenue FROM (SELECT l_partkey, l_extendedprice * (###_A - l_discount) AS rev_a, l_extendedprice * (###_C - l_discount) AS rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) l JOIN part p ON l.l_partkey = p.p_partkey;
Query#6: WITH l AS (SELECT l_partkey, l_extendedprice * (###_A - l_discount) AS rev_a, l_extendedprice * (###_C - l_discount) AS rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.rev_a ELSE ###_B END) / sum(l.rev_c) AS promo_revenue FROM l JOIN part p ON l.l_partkey = p.p_partkey;
Query#7: WITH l AS MATERIALIZED (SELECT l_partkey, count(*) AS line_cnt, sum(l_extendedprice * (###_A - l_discount)) AS sum_rev_a, sum(l_extendedprice * (###_C - l_discount)) AS sum_rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH GROUP BY l_partkey) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.sum_rev_a ELSE l.line_cnt * ###_B END) / sum(l.sum_rev_c) AS promo_revenue FROM l JOIN part p ON l.l_partkey = p.p_partkey;
Query#8: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.rev_a ELSE ###_B END) / sum(l.rev_c) AS promo_revenue FROM (SELECT l_partkey, l_extendedprice * (###_A - l_discount) AS rev_a, l_extendedprice * (###_C - l_discount) AS rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) l JOIN (SELECT p_partkey, p_type FROM part) p ON l.l_partkey = p.p_partkey;
Query#9: WITH l AS (SELECT l_partkey, l_extendedprice * (###_A - l_discount) AS rev_a, l_extendedprice * (###_C - l_discount) AS rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH), p AS (SELECT p_partkey, p_type FROM part) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.rev_a ELSE ###_B END) / sum(l.rev_c) AS promo_revenue FROM l JOIN p ON l.l_partkey = p.p_partkey;
Query#10: WITH dates AS (SELECT date &&&_B AS start_date, date &&&_C + interval &&&_D MONTH AS end_date), l AS (SELECT l_partkey, count(*) AS line_cnt, sum(l_extendedprice * (###_A - l_discount)) AS sum_rev_a, sum(l_extendedprice * (###_C - l_discount)) AS sum_rev_c FROM lineitem CROSS JOIN dates WHERE l_shipdate >= dates.start_date AND l_shipdate < dates.end_date GROUP BY l_partkey) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.sum_rev_a ELSE l.line_cnt * ###_B END) / sum(l.sum_rev_c) AS promo_revenue FROM l JOIN part p ON l.l_partkey = p.p_partkey;
Query#11: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.l_extendedprice * (###_A - l.l_discount) ELSE ###_B END) / sum(l.l_extendedprice * (###_C - l.l_discount)) AS promo_revenue FROM lineitem l JOIN part p ON l.l_partkey = p.p_partkey WHERE l.l_shipdate >= date &&&_B AND l.l_shipdate < date &&&_C + interval &&&_D MONTH;
Query#12: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN rev_a ELSE ###_B END) / sum(rev_c) AS promo_revenue FROM (SELECT l_partkey, l_extendedprice * (###_A - l_discount) AS rev_a, l_extendedprice * (###_C - l_discount) AS rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) l JOIN part p ON l.l_partkey = p.p_partkey;
Query#13: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.l_extendedprice * (###_A - l.l_discount) ELSE ###_B END) / sum(l.l_extendedprice * (###_C - l.l_discount)) AS promo_revenue FROM part p JOIN lineitem l ON p.p_partkey = l.l_partkey WHERE l.l_shipdate >= date &&&_B AND l.l_shipdate < date &&&_C + interval &&&_D MONTH;
Query#14: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.sum_rev_a ELSE l.line_cnt * ###_B END) / sum(l.sum_rev_c) AS promo_revenue FROM (SELECT l_partkey, count(*) AS line_cnt, sum(l_extendedprice * (###_A - l_discount)) AS sum_rev_a, sum(l_extendedprice * (###_C - l_discount)) AS sum_rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH GROUP BY l_partkey) l JOIN (SELECT p_partkey, p_type FROM part) p ON l.l_partkey = p.p_partkey;
Query#15: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.l_extendedprice * (###_A - l.l_discount) ELSE ###_B END) / sum(l.l_extendedprice * (###_C - l.l_discount)) AS promo_revenue FROM (SELECT l_partkey, l_extendedprice, l_discount FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < (date &&&_C + interval &&&_D MONTH)) l JOIN part p ON l.l_partkey = p.p_partkey;
Query#16: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.rev_a ELSE ###_B END) / NULLIF(sum(l.rev_c), 0) AS promo_revenue FROM (SELECT l_partkey, l_extendedprice * (###_A - l_discount) AS rev_a, l_extendedprice * (###_C - l_discount) AS rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH) l JOIN part p ON l.l_partkey = p.p_partkey;
Query#17: WITH l AS (SELECT l_partkey, count(*) AS line_cnt, sum(l_extendedprice * (###_A - l_discount)) AS sum_rev_a, sum(l_extendedprice * (###_C - l_discount)) AS sum_rev_c FROM lineitem WHERE l_shipdate >= date &&&_B AND l_shipdate < date &&&_C + interval &&&_D MONTH GROUP BY l_partkey) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN l.sum_rev_a ELSE l.line_cnt * ###_B END) / NULLIF(sum(l.sum_rev_c), 0) AS promo_revenue FROM l JOIN part p ON l.l_partkey = p.p_partkey;
Query#18: SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN lat.rev_a ELSE ###_B END) / sum(lat.rev_c) AS promo_revenue FROM lineitem l JOIN part p ON l.l_partkey = p.p_partkey CROSS JOIN LATERAL (SELECT l.l_extendedprice * (###_A - l.l_discount) AS rev_a, l.l_extendedprice * (###_C - l_discount) AS rev_c) lat WHERE l.l_shipdate >= date &&&_B AND l.l_shipdate < date &&&_C + interval &&&_D MONTH;
Query#19: WITH d AS (SELECT date &&&_B AS s, date &&&_C + interval &&&_D MONTH AS e) SELECT ^^^_A * sum(CASE WHEN p.p_type LIKE &&&_A THEN c.ra ELSE ###_B END) / sum(c.rc) AS promo_revenue FROM lineitem l JOIN d ON l.l_shipdate >= d.s AND l.l_shipdate < d.e JOIN part p ON l.l_partkey = p.p_partkey CROSS JOIN LATERAL (SELECT l.l_extendedprice * (###_A - l.l_discount) AS ra, l.l_extendedprice * (###_C - l.l_discount) AS rc) c;
Query#20: SELECT ^^^_A * sum(CASE WHEN part.p_type LIKE &&&_A THEN lineitem.l_extendedprice * (###_A - lineitem.l_discount) ELSE ###_B END) / sum(lineitem.l_extendedprice * (###_C - lineitem.l_discount)) AS promo_revenue FROM lineitem INNER JOIN part ON lineitem.l_partkey = part.p_partkey WHERE lineitem.l_shipdate >= date &&&_B AND lineitem.l_shipdate < date &&&_C + interval &&&_D MONTH;